load 'disk_drag.mat'
load 'cup_drag.mat'
load 'vel.mat'

rho = 1.23; % kg/m^3
d = 3 * 0.0254; % diameter of objects in m
A = pi * d^2/4; % area

% calculate Cd values
Cd_disk = 2 * disk_drag / (rho * vel.^2 * A)